import { StyleSheet } from 'react-native';

export const mainStyles = StyleSheet.create({
  container: {
    flexGrow: 1, // ScrollView의 컨텐츠 크기에 맞게 확장
    backgroundColor: '#121212',  // 다크 테마 배경
    padding:  22,
    paddingTop: 40,
  
  },
  // 추가 스타일 정의
});
